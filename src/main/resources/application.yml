# This file groups application.properties
# to Markdown .yml file: application.yml
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/mydb
    username: root
    password: carlospdl%
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        generate_statistics: false
      show-sql: true
      format_sql: false
    #Turn Statics On
    logging:
      level:
        org.hibernate.stat: debug
  sql:
    init:
      mode: always